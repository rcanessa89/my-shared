// import { Loading } from './loading';
// import { render } from '../../test-utils';

describe('Loading', () => {
  it('should render successfully', () => {
    // const { baseElement } = render(<Loading show />);
    expect(true).toBeTruthy();
  });
});
